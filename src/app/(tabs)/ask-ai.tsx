import { View } from 'react-native';

// This is a placeholder - the actual Ask AI modal is in /modal
// The tab button opens the modal instead of navigating here
export default function AskAIPlaceholder() {
  return <View />;
}
